# {{ project_name }}

> Add your project description here.

## Quick Start

```bash
git clone <your-repo-url>
cd {{ project_name | lower | replace(' ', '-') }}
./bootstrap.sh
{% if lang_python %}source .venv/bin/activate
{% endif %}```

## Development

```bash
make help              # Show all targets
{% if lang_python %}make lint              # Lint Python with ruff
make test              # Run Python tests
make test-coverage     # Tests + coverage report
make security-scan     # Bandit scan
{% endif %}{% if lang_typescript %}make lint-ts           # Lint TypeScript
make test-ts           # Run TypeScript tests
{% endif %}{% if lang_go %}make lint-go           # Lint Go
make test-go           # Run Go tests
{% endif %}{% if lang_rust %}make lint-rust          # Lint Rust
make test-rust         # Run Rust tests
{% endif %}make lint-all          # Run all linters
make test-all          # Run all tests
make context-refresh   # Update AI context files
make hooks-run         # Run all pre-commit hooks
{% if mempalace_enabled %}make memory-status     # MemPalace status
make context-refresh-live  # Sync MemPalace to context files
{% endif %}```

## Project Structure

```
{{ project_name | lower | replace(' ', '-') }}/
├── {{ source_dirs }}                # Source code
├── tests/                           # Test suite
├── .github/
│   ├── instructions/                # Workflow playbooks
│   ├── context/                     # AI agent context files
│   └── workflows/                   # CI/CD pipelines
├── docs/                            # Documentation (analysis, decisions, plans, implementations)
{% if lang_python %}├── .pre-commit-config.yaml          # Mechanical guardrails
{% endif %}├── Makefile                         # Build targets
├── bootstrap.sh                     # One-time setup
└── CONTRIBUTING.md                  # How to contribute
```

## Workflow

This project uses the KEEL tiered workflow. See [CONTRIBUTING.md](CONTRIBUTING.md) for details.

| Tier | Scope | Review |
|------|-------|--------|
| **S** | Schema, security, infra | Human required |
| **A** | New features | Human required |
| **B** | Enhancements | Auto-merge if CI green |
| **C** | Docs, typos | Auto-merge if CI green |
{% if gstack_enabled %}
Use `/review` before PRs and `/ship` to land changes.
{% endif %}
## Contributing

See [CONTRIBUTING.md](CONTRIBUTING.md).

## License

See [LICENSE](LICENSE).
