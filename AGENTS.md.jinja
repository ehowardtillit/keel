{% if skip_agent_instructions %}
# {{ project_name }}

This project uses a custom engineering methodology. Agent instructions are provided by the project's methodology system, not by KEEL.

Read the methodology's own instruction files for workflow, guardrails, and conventions.
{% else %}
# {{ project_name }}

**Customize:** Replace this with a short description of the product, main components, and how this repository is organized.

## Read first

- `.github/context/*.md` — architecture, API surface, module map, conventions, recent changes
- `docs/` — decisions (`docs/decisions/`), plans, implementation notes

Primary source directories: `{{ source_dirs }}`

{%- set langs = [] -%}
{%- if lang_python %}{% set _ = langs.append("Python") %}{% endif -%}
{%- if lang_typescript %}{% set _ = langs.append("TypeScript") %}{% endif -%}
{%- if lang_go %}{% set _ = langs.append("Go") %}{% endif -%}
{%- if lang_rust %}{% set _ = langs.append("Rust") %}{% endif -%}
{%- if lang_php %}{% set _ = langs.append("PHP") %}{% endif -%}
{%- if lang_elixir %}{% set _ = langs.append("Elixir") %}{% endif -%}
{%- if langs %}
Languages in this project: {{ langs | join(", ") }}.
{%- endif %}

{% if not custom_methodology %}
## Tiered workflow (S / A / B / C)

All work: branch, PR, tests, and lint. Tier sets ceremony and who merges.

| Tier | Scope | Flow | Review |
|------|-------|------|--------|
| **S** | Schema, security, infra, auth, deploy | Full ceremony | Human review |
| **A** | Features (new capability, API, integration) | Issue + PR | Human review |
| **B** | Enhancements to existing behavior | PR | Auto-merge if CI green |
| **C** | Trivial (docs, typos, comments) | PR | Auto-merge if CI green |

Conventional commits: `feat|fix|docs|refactor|test|chore|ci|perf: description`

{% endif %}
{% if not custom_methodology %}
## Guardrails

**NEVER:** Drop or wipe databases; delete infrastructure (CI workflows, deploy scripts, `keel`, `keel.yml`); push to `main`; skip tests to save time; store secrets or commit credentials; leave TODO/FIXME/HACK in committed code; add ghost implementations (unwired or untested code).

**ALWAYS:** Run tests before and after changes; update `.github/context/` when architecture or public surface changes; verify every import resolves; keep functions under 50 lines; prefer editing existing files over adding new ones.

{% endif %}
{% if mempalace_enabled -%}
## MemPalace

MemPalace MCP is available. Query `mempalace_search` before making assumptions about past decisions.

{% endif %}
{% if gstack_enabled -%}
## gstack

gstack skills are available for sprint workflow.

{% endif %}
{% if runtime_enforcement != "none" or merge_gate != "none" %}
## Project enforcement

{% if runtime_enforcement != "none" %}
Runtime guardrails: **{{ runtime_enforcement }}**.
{% endif %}
{% if merge_gate != "none" %}
Merge gate: **{{ merge_gate }}**.
{% endif %}
{% endif %}

## Detailed playbooks

See `.github/instructions/` for full details:
- `.github/instructions/guardrails.md` — full NEVER/ALWAYS list and LLM verification
- `.github/instructions/code-review.md` — review checklist (7 dimensions)
{%- if not custom_methodology %}
- `.github/instructions/workflow.md` — tier ceremonies and tickets
{%- endif %}
- `.github/instructions/prompts.md` — prompt patterns
- `.github/instructions/processes.md` — delegation, testing, orchestration

{% endif %}
